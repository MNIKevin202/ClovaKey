{
  "identifier": "default",
  "description": "Least-privilege capability set for the ClovaKey main window. ClovaKey works fully offline; the frontend is granted no network, filesystem, or shell access — only user-driven dialogs, a single clipboard-write for the reveal flow, read-only OS info, and autostart toggling. All secret handling and file IO happen in Rust.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:allow-version",
    "core:window:allow-start-dragging",
    "clipboard-manager:allow-write-text",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "os:allow-platform",
    "os:allow-version",
    "os:allow-os-type",
    "os:allow-arch",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
